<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:pv="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="AhiaJara.Utils.AdsCarousel">
    <Grid x:Name="mainStack">
        <CarouselView
            VerticalOptions="Fill"
        x:Name="carouselView"
            HeightRequest="150"
            IsScrollAnimated="True"
            ItemsSource="{Binding CarouselModelList}"
            ItemsUpdatingScrollMode="KeepItemsInView"
            IndicatorView="indicatorView">
            <CarouselView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <pv:PancakeView CornerRadius="15" HorizontalOptions="Fill">
                            <Image Source="{Binding Image}" Aspect="AspectFill"/>
                        </pv:PancakeView>
                    </Grid>
                </DataTemplate>
            </CarouselView.ItemTemplate>
        </CarouselView>
        <IndicatorView
            x:Name="indicatorView"
         Padding="0,0,0,3"
         HorizontalOptions="Center"
         IndicatorColor="White"
            Margin="0,0,0,3"
         IndicatorSize="7"
         SelectedIndicatorColor="Accent"
         VerticalOptions="End" />
    </Grid>
</ContentView>